import {createClient} from '@/lib/supabase/server';
export default async function NewsletterAdmin(){const {data:rows,error}=await createClient().from('newsletter_subscribers').select('id,name,email,status,source,created_at').order('created_at',{ascending:false});return <div className="admin-content"><div className="admin-heading"><div><p className="eyebrow">THE ADRIELY JOURNAL</p><h1 className="serif">Newsletter <i>Subscribers.</i></h1><p className="admin-lead">Leads captados pelo site e sincronizados com Resend quando a integração estiver configurada.</p></div><div className={process.env.RESEND_API_KEY?'status-pill on':'status-pill'}>Resend · {process.env.RESEND_API_KEY?'conectado':'não configurado'}</div></div><div className="table-wrap">{error?<p>Configure a tabela newsletter_subscribers no Supabase.</p>:<table><thead><tr><th>Nome</th><th>E-mail</th><th>Status</th><th>Origem</th><th>Data</th></tr></thead><tbody>{(rows??[]).map(r=><tr key={r.id}><td>{r.name||'—'}</td><td>{r.email}</td><td>{r.status}</td><td>{r.source}</td><td>{new Date(r.created_at).toLocaleDateString('pt-BR')}</td></tr>)}</tbody></table>}</div><div className="setup-note"><b>Próximo passo:</b> quando você configurar <code>RESEND_API_KEY</code>, novos inscritos passam a ser enviados automaticamente para os contatos do Resend. Nenhuma chave é necessária para construir o painel.</div></div>}
