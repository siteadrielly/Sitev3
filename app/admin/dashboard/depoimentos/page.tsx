import Link from "next/link";import {createClient} from "@/lib/supabase/server";import {deleteTestimonial} from "@/lib/admin-actions-content";export const dynamic="force-dynamic";
export default async function Testimonials(){const {data:rows}=await createClient().from("testimonials").select("id,name,city,text,created_at").order("created_at",{ascending:false});return <div className="admin-content"><div className="admin-heading"><div><p className="eyebrow">PROVA SOCIAL</p><h1 className="serif">Depoimentos.</h1><p className="admin-lead">Relatos autorizados para transmitir a experiência de cuidado da clínica.</p></div><Link href="/admin/dashboard/novo-depoimento" className="btn btn-olive">+ Novo depoimento</Link></div><div className="table-wrap"><table><thead><tr><th>Paciente</th><th>Relato</th><th>Data</th><th></th></tr></thead><tbody>{rows?.length?rows.map(r=><tr key={r.id}><td>{r.name}{r.city?` — ${r.city}`:""}</td><td>{r.text}</td><td>{new Date(r.created_at).toLocaleDateString("pt-BR")}</td><td><form action={deleteTestimonial.bind(null,r.id)}><button className="admin-delete">Apagar</button></form></td></tr>):<tr><td colSpan={4}>Nenhum depoimento cadastrado ainda.</td></tr>}</tbody></table></div></div>}
