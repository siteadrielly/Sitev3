"use client";
import {useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const {error}=await createClient().auth.signInWithPassword({email,password});if(error)setError(error.message);else window.location.href='/admin/dashboard';setLoading(false)}return <main className="admin-auth"><div className="auth-card"><p className="eyebrow">PAINEL VITAL</p><h1 className="serif">Adriely <i>Anute.</i></h1><p>Acesso administrativo</p><form onSubmit={submit}><label>E-mail<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><label>Senha<input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></label><button className="btn btn-olive" disabled={loading}>{loading?'Entrando…':'Entrar no painel'}</button>{error&&<small className="form-error">{error}</small>}</form><a href="/">← Voltar ao site</a></div></main>}
