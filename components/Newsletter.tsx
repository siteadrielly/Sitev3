"use client";
import {FormEvent,useState} from "react";
export default function Newsletter(){const [status,setStatus]=useState('idle');const [message,setMessage]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus('loading');const form=new FormData(e.currentTarget);const res=await fetch('/api/newsletter',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:String(form.get('name')||''),email:String(form.get('email')||''),website:String(form.get('website')||'')})});const data=await res.json();if(!res.ok){setStatus('error');setMessage(data.error||'Não foi possível concluir agora.');return}setStatus('success');setMessage('Pronto. Você entrou para o Journal.');e.currentTarget.reset()}return <section id="newsletter" className="newsletter section"><div className="container newsletter-grid"><div><p className="eyebrow">THE ADRIELY JOURNAL</p><h2 className="serif">Informação também é<br/><i>cuidado.</i></h2><p>Conteúdos selecionados sobre pele, longevidade, harmonização, estética e autocuidado — sem excesso, sem ruído.</p></div><form onSubmit={submit}><label>Seu nome<input name="name" placeholder="Como podemos chamar você?"/></label><label>Seu melhor e-mail<input name="email" type="email" required placeholder="voce@email.com"/></label><input className="honeypot" name="website" tabIndex={-1} autoComplete="off"/><button className="btn btn-olive" disabled={status==='loading'}>{status==='loading'?'Enviando…':'Entrar para o Journal →'}</button>{message&&<p className={status==='error'?'form-error':'form-success'}>{message}</p>}<small>Ao se cadastrar, você concorda em receber conteúdos da Dra. Adriely Anute. Você pode sair quando quiser.</small></form></div></section>}
