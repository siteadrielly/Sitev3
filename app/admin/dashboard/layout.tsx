import Link from 'next/link';import {createClient} from '@/lib/supabase/server';import {signOut} from '@/lib/admin-actions';
export default async function Layout({children}:{children:React.ReactNode}){const {data:{user}}=await createClient().auth.getUser();return <div className="admin-shell"><aside><p className="eyebrow">PAINEL VITAL</p><h2 className="serif">Adriely <i>Anute</i></h2><nav><Link href="/admin/dashboard">Visão geral</Link><Link href="/admin/dashboard/newsletter">Newsletter</Link><Link href="/admin/dashboard/posts">Conteúdo</Link></nav><div className="admin-user"><small>{user?.email}</small><form action={signOut}><button>Sair</button></form></div></aside><main>{children}</main></div>}
