export const dynamic="force-dynamic";
export default function Analytics(){const configured=Boolean(process.env.CLOUDFLARE_ANALYTICS_API_TOKEN&&process.env.CLOUDFLARE_ACCOUNT_ID);return <div className="admin-content"><p className="eyebrow">DESEMPENHO</p><h1 className="serif">Analytics.</h1><p className="admin-lead">Acompanhe as visitas e o desempenho do novo site pelo Cloudflare Web Analytics.</p><div className="admin-cards"><div><span>STATUS</span><strong>{configured?"ON":"OFF"}</strong><small>{configured?"integração configurada":"configure as chaves do Cloudflare"}</small></div><div><span>PERÍODO</span><strong>14</strong><small>dias de histórico</small></div><div><span>PRÓXIMO</span><strong>RUM</strong><small>dados de visitas e páginas</small></div></div>{!configured&&<div className="setup-note"><b>Para ativar:</b> adicione <code>CLOUDFLARE_ANALYTICS_API_TOKEN</code> e <code>CLOUDFLARE_ACCOUNT_ID</code> aos secrets do GitHub Actions.</div>}</div>}
