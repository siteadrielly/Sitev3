import Link from "next/link";import {createClient} from "@/lib/supabase/server";import {deleteBeforeAfter,seedBeforeAfterAcervo} from "@/lib/admin-actions-content";export const dynamic="force-dynamic";
export default async function BeforeAfter({searchParams}:{searchParams:{imported?:string}}){const {data:cases}=await createClient().from("before_after").select("id,procedure,published,created_at,image_url").order("created_at",{ascending:false});return <div className="admin-content"><div className="admin-heading"><div><p className="eyebrow">RESULTADOS</p><h1 className="serif">Antes & <i>depois.</i></h1><p className="admin-lead">Gerencie o acervo de resultados. Publique somente imagens com autorização para divulgação.</p></div><div className="admin-actions"><form action={seedBeforeAfterAcervo}><button className="btn">Importar acervo</button></form><Link href="/admin/dashboard/novo-antes-depois" className="btn btn-olive">+ Novo caso</Link></div></div>{searchParams.imported&&<div className="setup-note">Acervo importado: {searchParams.imported} novos casos.</div>}<div className="table-wrap"><table><thead><tr><th>Procedimento</th><th>Status</th><th>Data</th><th></th></tr></thead><tbody>{cases?.length?cases.map(c=><tr key={c.id}><td>{c.procedure}</td><td>{c.published?"Publicado":"Rascunho"}</td><td>{new Date(c.created_at).toLocaleDateString("pt-BR")}</td><td><form action={deleteBeforeAfter.bind(null,c.id)}><button className="admin-delete">Apagar</button></form></td></tr>):<tr><td colSpan={4}>Nenhum resultado cadastrado ainda.</td></tr>}</tbody></table></div></div>}
