import Link from "next/link";
import { createClient } from "@/lib/supabase/server";
import { deletePost } from "@/lib/admin-actions-content";
export const dynamic="force-dynamic";
export default async function Posts(){const {data:posts}=await createClient().from("posts").select("id,title,category,published,created_at").order("created_at",{ascending:false});return <div className="admin-content"><div className="admin-heading"><div><p className="eyebrow">CONTEÚDO</p><h1 className="serif">Posts do <i>blog.</i></h1><p className="admin-lead">Publique conteúdos educativos sobre pele, estética, longevidade e harmonização.</p></div><Link href="/admin/dashboard/novo-post" className="btn btn-olive">+ Novo post</Link></div><div className="table-wrap"><table><thead><tr><th>Título</th><th>Categoria</th><th>Status</th><th>Data</th><th></th></tr></thead><tbody>{posts?.length?posts.map(p=><tr key={p.id}><td>{p.title}</td><td>{p.category||"—"}</td><td>{p.published?"Publicado":"Rascunho"}</td><td>{new Date(p.created_at).toLocaleDateString("pt-BR")}</td><td><form action={deletePost.bind(null,p.id)}><button className="admin-delete">Apagar</button></form></td></tr>):<tr><td colSpan={5}>Nenhum post cadastrado ainda.</td></tr>}</tbody></table></div></div>}
